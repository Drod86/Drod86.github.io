// const toggleMenu = (el, open) => {
//   const length = el.classList.length;
//   const lastClass = el.classList[length - 1];
//   lastClass === open ? el.classList.remove(open) : el.classList.add(open);
// };

// export default toggleMenu;
